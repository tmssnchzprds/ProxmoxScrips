# Configuración mínima para master.sh
# Edita según necesites
PARALLEL=4
# Rutas de repos (relativas a este repo)
REPOS=(amd64 arm64)

# Proxmox API settings
# URL del API (incluye protocolo y puerto): https://proxmox.example.com:8006
PM_API_URL="https://proxmox.example.com:8006"
# API token format: user@realtokenidm!=secret
# Ejemplo: PVEAPITOKEN="root@pam!mytoken=abcdef123456"
PVEAPITOKEN="root@pam!mytoken=abcdef123456"
